#include<iostream>
using namespace std;
#define ll long long
int travelcard()
{
	int n, a, b, k, f;
	cin >> n >> a >> b >> k >> f;
	map<pair<string, string>, int>m;
	strin prev = "";
	int price = 0;
	for (int i = 0; i < n; i++)
	{
		string start_dest, end_dest;
		cin >> start_dest >> end_dest;
		int price;
		if (prev == start_prev)
		{
			price = b;
		}
		else
		{
			price = a;
		}
		if ()
		}
	int main()
	{
		travelcard()